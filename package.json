{
  "name": "mathgame",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android",
    "android:build": "cap sync android && cap open android"
  },
  "dependencies": {
    "vercel": "^56.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
